syntax on
filetype plugin indent on

set number

" indent / tabs (matches Zed: 2 spaces)
set tabstop=2 shiftwidth=2 expandtab

" search
set hlsearch incsearch
set ignorecase smartcase

" usability
set backspace=indent,eol,start
set mouse=a
set clipboard=unnamed
set noswapfile
